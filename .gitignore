# Ignore everything by default
*

# Allow gitignore itself
!.gitignore

# Allow local notebook
!local.ipynb

# Allow outputs/logs and outputs/checkpoints (not tracks)
!outputs/
!outputs/logs/
!outputs/logs/**
!outputs/checkpoints/
!outputs/checkpoints/**
